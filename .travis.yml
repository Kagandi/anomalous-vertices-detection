language: python
python:
  - "2.7"
# command to install dependencies
install:
  - pip install --upgrade pip
  - pip install pipenv
  - pipenv install --dev
# command to run tests
script:
  - python -m unittest # or py.test for Python versions 3.5 and below